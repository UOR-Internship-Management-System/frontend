# State Management

Sprint 1 uses TanStack Query for server-state foundation only. No feature-specific queries are active.

Client UI state is provider-scoped: minimal foundation auth state, theme preference, notifications, and overlays.
