# Frontend Test Plan

Sprint 1 tests cover app rendering, route guard redirects, unauthorized/fallback rendering, shared UI primitive rendering, OpenAPI sync, environment validation, and removed-scope scanning.

Playwright specs are route-smoke tests only.
