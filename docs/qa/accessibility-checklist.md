# Accessibility Checklist

- Use semantic landmarks for header, nav, main, and footer.
- Keep buttons keyboard focusable with visible focus styles.
- Use labelled inputs in shared form primitives.
- Keep modal foundations marked with `role="dialog"` and `aria-modal="true"`.
- Preserve color contrast in light and dark themes.
