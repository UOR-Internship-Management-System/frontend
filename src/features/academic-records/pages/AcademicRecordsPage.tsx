import { ShellPage } from '../../foundation/ShellPage'

export function AcademicRecordsPage() {
  return (
    <ShellPage
      description="Student academic records view shell for later read-only integration."
      title="Academic Records"
    />
  )
}
