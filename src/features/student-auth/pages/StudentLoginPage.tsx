import { ShellPage } from '../../foundation/ShellPage'

export function StudentLoginPage() {
  return (
    <ShellPage
      description="Student login route shell without session lifecycle implementation."
      title="Student Login"
    />
  )
}
